# Laravel API base URL (must include /api)
VITE_API_URL= https://floppy-cities-dig.loca.lt

# Optional: same host/port as Laravel but without /api (helps relative /storage/... URLs if API ever returns them)
# VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
